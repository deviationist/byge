import { AddLocationScreen } from "../../screens/AddLocationScreen";

// Edit is the same screen in a second mode — one component, two modes. It reads
// `id` from the route and prefills; nothing else differs.
export default AddLocationScreen;
