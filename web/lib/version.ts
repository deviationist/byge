/** Read from package.json at build time is not available to Metro without a
 * plugin, so the number lives here and is stated once. It appears on the About
 * screen, where a version that lies is worse than no version at all. */
export const APP_VERSION = "0.1.0";
